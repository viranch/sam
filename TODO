TODO List:

1. MainWindow is in tray, open settings from tray menu, click OK or Cancel, SAM segfaults. Fix this.

2. Have dynamic quota limit. Right now SAM deals only with 100MB quota limit. Handle other limits and no-limit cases.
